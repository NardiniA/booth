---
label: Gallery Slider
hide_body: true
fields:
- name: gallery
  type: field_group_list
  fields:
  - name: image
    type: file
    config:
      maxSize: 64
    label: Image
  - name: alt
    type: text
    config:
      required: true
    label: Image Description
    description: Describe the image above
  config:
    min: 1
    max: 
    labelField: alt
  label: Gallery
